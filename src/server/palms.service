[Unit]
Description=PALMS (Precision Accurate LIBS Movement System)
After=multi-user.target

[Service]
Type=simple
ExecStart=python3 -u -m server
Environment="LOG_LEVEL=INFO"
Restart=always

[Install]
WantedBy=multi-user.target
